license="${mod_license}"
issueTrackerURL="${issue_tracker_url}"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
updateJSONURL="${update_json_url}"
displayURL="${display_url}"
logoFile="logo.png"
credits="${mod_credits}"
authors="${mod_authors}"
description='''
Every mob attacks you on sight — passive, aquatic, flying, all of them. Fully configurable: damage and speed multipliers, call for help, long-term memory, dimension blacklist, entity whitelist/blacklist, datapack-friendly exclusion tag, reactive-only mode, and per-player exemptions. Server-only, vanilla clients can join.
'''

[[dependencies.${mod_id}]]
    modId="neoforge"
    type="required"
    versionRange="[${neo_version},)"
    ordering="NONE"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="minecraft"
    type="required"
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"
