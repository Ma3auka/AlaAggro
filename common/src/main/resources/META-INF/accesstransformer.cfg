# Mob.goalSelector / Mob.targetSelector are protected in vanilla. Rewriting a mob's brain is the
# whole point of this mod, and shared code compiles against plain Minecraft, so both need widening.
# The Fabric side does the same through alaaggro.accesswidener; keep the two files in step.
public net.minecraft.world.entity.Mob goalSelector
public net.minecraft.world.entity.Mob targetSelector
